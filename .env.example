# Sola Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git (it's in .gitignore)

# =============================================================================
# CLIENT-SIDE VARIABLES (bundled into the app)
# These are prefixed with EXPO_PUBLIC_ and will be visible in the app bundle
# =============================================================================

# Supabase project URL
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public key (safe to expose - RLS protects data)
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# PostHog analytics key
EXPO_PUBLIC_POSTHOG_KEY=your-posthog-key

# =============================================================================
# SERVER-SIDE VARIABLES (NOT bundled - used only by scripts and edge functions)
# NEVER add EXPO_PUBLIC_ prefix to these - they must stay server-side only
# =============================================================================

# Supabase service role key - FULL DATABASE ACCESS, BYPASSES RLS
# Only used by: scripts/seed-*.ts, scripts/verify-*.ts, supabase/functions/
# WARNING: Never expose this in client code or add EXPO_PUBLIC_ prefix
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# External API keys for seeding scripts
PEXELS_API_KEY=your-pexels-key
GOOGLE_PLACES_API_KEY=your-google-places-key
